# One service: build the render-site image and serve it on http://localhost:3000.
#   docker compose up --build
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: juniresearch-web
    ports:
      - "3000:3000"
    restart: unless-stopped
